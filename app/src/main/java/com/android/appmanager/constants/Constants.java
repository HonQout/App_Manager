package com.android.appmanager.constants;

public class Constants {
    public enum AppScope {
        All, System, User
    }

    public enum PackageInfoScope {
        Overview, PermissionDefined, PermissionRequested, Activity, Service, Provider, Receiver
    }
}